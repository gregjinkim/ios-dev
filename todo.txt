Need to gather user info during sign-in and store in users database

Have view controller populated when switching between views

Show the other user’s information in view ratings
	Added picture, still need age, location, occupation, race

Create multiple swift files for each ViewController

